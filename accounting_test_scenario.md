# 🧪 سيناريو الاختبار الشامل - وحدة المحاسبة

**التاريخ:** 13 نوفمبر 2025  
**المشروع:** نظام العباسي الموحد  
**الوحدة:** Accounting Module  
**الهدف:** التحقق من سلامة القيود اليومية ودليل الحسابات

---

## 📋 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [البيئة الاختبارية](#البيئة-الاختبارية)
3. [البيانات التجريبية](#البيانات-التجريبية)
4. [سيناريوهات الاختبار](#سيناريوهات-الاختبار)
5. [معايير النجاح](#معايير-النجاح)
6. [تنفيذ الاختبار](#تنفيذ-الاختبار)

---

## 🎯 نظرة عامة

يهدف هذا السيناريو إلى اختبار شامل لوحدة المحاسبة في نظام العباسي الموحد، مع التركيز على:

- ✅ **دليل الحسابات** (Chart of Accounts)
- ✅ **الحسابات التحليلية** (Analytical Accounts)
- ✅ **القيود اليومية** (Journal Entries)
- ✅ **سندات القبض والصرف** (Receipt & Payment Vouchers)
- ✅ **التوازن المحاسبي** (Accounting Balance)
- ✅ **العملات المتعددة** (Multi-Currency)
- ✅ **Multi-Tenancy** (الوحدات المحاسبية المتعددة)

---

## 🖥️ البيئة الاختبارية

### المتطلبات

```bash
- MySQL 8.0+
- Node.js 22.x
- pnpm 10.x
- نظام العباسي الموحد (alabasi-unified-system)
```

### إعداد البيئة

```bash
# 1. استنساخ المشروع
git clone https://github.com/alabasi2025/alabasi-unified-system.git
cd alabasi-unified-system

# 2. تثبيت المكتبات
pnpm install

# 3. إعداد قاعدة البيانات
pnpm db:push

# 4. تشغيل النظام
pnpm dev
```

---

## 📊 البيانات التجريبية

### 1. البيانات الأساسية (Core Data)

#### **1.1 العملات (Currencies)**

```sql
INSERT INTO currencies (code, name_ar, name_en, symbol, exchange_rate) VALUES
('YER', 'ريال يمني', 'Yemeni Rial', 'ر.ي', 1.00),
('USD', 'دولار أمريكي', 'US Dollar', '$', 1350.00),
('SAR', 'ريال سعودي', 'Saudi Riyal', 'ر.س', 360.00),
('EUR', 'يورو', 'Euro', '€', 1450.00);
```

#### **1.2 المؤسسة (Organization)**

```sql
INSERT INTO organizations (name_ar, name_en, tax_number, phone, email, address) VALUES
('شركة العباسي التجارية', 'Alabasi Trading Company', 'TAX-12345', '+967-1-234567', 'info@alabasi.com', 'صنعاء، اليمن');
```

#### **1.3 الفرع (Branch)**

```sql
INSERT INTO branches (organization_id, name_ar, name_en, phone, email, address) VALUES
(1, 'الفرع الرئيسي', 'Main Branch', '+967-1-234567', 'main@alabasi.com', 'شارع الزبيري، صنعاء');
```

#### **1.4 الوحدة المحاسبية (Unit)**

```sql
INSERT INTO units (organization_id, branch_id, name_ar, name_en, start_date, fiscal_year_start) VALUES
(1, 1, 'الوحدة المحاسبية الرئيسية', 'Main Accounting Unit', '2025-01-01', '2025-01-01');
```

---

### 2. دليل الحسابات (Chart of Accounts)

#### **2.1 أنواع الحسابات الرئيسية (Account Types)**

```sql
INSERT INTO account_types (code, name_ar, name_en, category, display_order) VALUES
('1', 'الأصول', 'Assets', 'asset', 1),
('2', 'الخصوم', 'Liabilities', 'liability', 2),
('3', 'حقوق الملكية', 'Equity', 'equity', 3),
('4', 'الإيرادات', 'Revenue', 'revenue', 4),
('5', 'المصروفات', 'Expenses', 'expense', 5);
```

#### **2.2 الحسابات الرئيسية (Parent Accounts)**

```sql
-- الأصول
INSERT INTO chart_of_accounts (unit_id, account_type_id, code, name_ar, name_en, is_parent, level) VALUES
(1, 1, '1', 'الأصول', 'Assets', true, 1),
(1, 1, '11', 'الأصول المتداولة', 'Current Assets', true, 2),
(1, 1, '111', 'النقدية والبنوك', 'Cash and Banks', true, 3);

-- الخصوم
INSERT INTO chart_of_accounts (unit_id, account_type_id, code, name_ar, name_en, is_parent, level) VALUES
(1, 2, '2', 'الخصوم', 'Liabilities', true, 1),
(1, 2, '21', 'الخصوم المتداولة', 'Current Liabilities', true, 2),
(1, 2, '211', 'الموردون', 'Suppliers', true, 3);

-- حقوق الملكية
INSERT INTO chart_of_accounts (unit_id, account_type_id, code, name_ar, name_en, is_parent, level) VALUES
(1, 3, '3', 'حقوق الملكية', 'Equity', true, 1),
(1, 3, '31', 'رأس المال', 'Capital', true, 2);

-- الإيرادات
INSERT INTO chart_of_accounts (unit_id, account_type_id, code, name_ar, name_en, is_parent, level) VALUES
(1, 4, '4', 'الإيرادات', 'Revenue', true, 1),
(1, 4, '41', 'إيرادات المبيعات', 'Sales Revenue', true, 2);

-- المصروفات
INSERT INTO chart_of_accounts (unit_id, account_type_id, code, name_ar, name_en, is_parent, level) VALUES
(1, 5, '5', 'المصروفات', 'Expenses', true, 1),
(1, 5, '51', 'مصروفات التشغيل', 'Operating Expenses', true, 2);
```

#### **2.3 الحسابات الفرعية (Sub Accounts)**

```sql
-- حسابات النقدية
INSERT INTO chart_of_accounts (unit_id, account_type_id, parent_id, code, name_ar, name_en, is_parent, level, analytical_type) VALUES
(1, 1, 3, '1111', 'الصناديق', 'Cash Boxes', false, 4, 'fund'),
(1, 1, 3, '1112', 'البنوك', 'Banks', false, 4, 'bank'),
(1, 1, 3, '1113', 'الصرافين', 'Cashiers', false, 4, 'cashier');

-- حسابات العملاء
INSERT INTO chart_of_accounts (unit_id, account_type_id, parent_id, code, name_ar, name_en, is_parent, level, analytical_type) VALUES
(1, 1, 2, '1121', 'العملاء', 'Customers', false, 4, 'customer');

-- حسابات الموردين
INSERT INTO chart_of_accounts (unit_id, account_type_id, parent_id, code, name_ar, name_en, is_parent, level, analytical_type) VALUES
(1, 2, 6, '2111', 'الموردون', 'Suppliers', false, 4, 'supplier');

-- حسابات المصروفات
INSERT INTO chart_of_accounts (unit_id, account_type_id, parent_id, code, name_ar, name_en, is_parent, level) VALUES
(1, 5, 11, '5101', 'الرواتب والأجور', 'Salaries and Wages', false, 3),
(1, 5, 11, '5102', 'الإيجارات', 'Rent', false, 3),
(1, 5, 11, '5103', 'الكهرباء والماء', 'Utilities', false, 3);

-- حسابات الإيرادات
INSERT INTO chart_of_accounts (unit_id, account_type_id, parent_id, code, name_ar, name_en, is_parent, level) VALUES
(1, 4, 9, '4101', 'مبيعات البضائع', 'Goods Sales', false, 3),
(1, 4, 9, '4102', 'إيرادات الخدمات', 'Service Revenue', false, 3);
```

#### **2.4 ربط العملات المفضلة بالحسابات**

```sql
-- حساب الصناديق يقبل YER فقط
INSERT INTO account_currencies (account_id, currency_id) VALUES
(13, 1); -- YER

-- حساب البنوك يقبل جميع العملات
INSERT INTO account_currencies (account_id, currency_id) VALUES
(14, 1), -- YER
(14, 2), -- USD
(14, 3), -- SAR
(14, 4); -- EUR

-- حساب الصرافين يقبل YER و USD
INSERT INTO account_currencies (account_id, currency_id) VALUES
(15, 1), -- YER
(15, 2); -- USD

-- حساب العملاء يقبل جميع العملات
INSERT INTO account_currencies (account_id, currency_id) VALUES
(16, 1), (16, 2), (16, 3), (16, 4);

-- حساب الموردين يقبل جميع العملات
INSERT INTO account_currencies (account_id, currency_id) VALUES
(17, 1), (17, 2), (17, 3), (17, 4);
```

---

### 3. الحسابات التحليلية (Analytical Accounts)

#### **3.1 الصناديق (Funds)**

```sql
INSERT INTO funds (unit_id, branch_id, account_id, code, name_ar, name_en, opening_balance, current_balance, status) VALUES
(1, 1, 13, 'CASH-001', 'الصندوق الرئيسي', 'Main Cash Box', 100000.00, 100000.00, 'active'),
(1, 1, 13, 'CASH-002', 'صندوق المبيعات', 'Sales Cash Box', 50000.00, 50000.00, 'active');

-- ربط العملات
INSERT INTO fund_currencies (fund_id, currency_id) VALUES
(1, 1), -- YER فقط
(2, 1); -- YER فقط
```

#### **3.2 البنوك (Banks)**

```sql
INSERT INTO banks (unit_id, branch_id, account_id, code, name_ar, name_en, bank_name, account_number, iban, swift_code, opening_balance, current_balance, status) VALUES
(1, 1, 14, 'BANK-001', 'حساب البنك الأهلي', 'National Bank Account', 'البنك الأهلي اليمني', '123456789', 'YE12345678901234567890', 'NBOYYE22', 500000.00, 500000.00, 'active'),
(1, 1, 14, 'BANK-002', 'حساب بنك التسليف', 'Taslif Bank Account', 'بنك التسليف والادخار', '987654321', 'YE98765432109876543210', 'TBSYYE22', 300000.00, 300000.00, 'active');

-- ربط العملات (جميع العملات)
INSERT INTO bank_currencies (bank_id, currency_id) VALUES
(1, 1), (1, 2), (1, 3), (1, 4),
(2, 1), (2, 2), (2, 3), (2, 4);
```

#### **3.3 الصرافين (Cashiers)**

```sql
INSERT INTO cashiers (unit_id, branch_id, account_id, code, name_ar, name_en, employee_name, phone, opening_balance, current_balance, status) VALUES
(1, 1, 15, 'CASH-001', 'الصراف أحمد', 'Cashier Ahmed', 'أحمد محمد', '+967-777-123456', 20000.00, 20000.00, 'active'),
(1, 1, 15, 'CASH-002', 'الصراف علي', 'Cashier Ali', 'علي حسن', '+967-777-654321', 15000.00, 15000.00, 'active');

-- ربط العملات (YER و USD)
INSERT INTO cashier_currencies (cashier_id, currency_id) VALUES
(1, 1), (1, 2),
(2, 1), (2, 2);
```

#### **3.4 العملاء (Customers)**

```sql
INSERT INTO customers (unit_id, branch_id, account_id, code, name_ar, name_en, tax_number, phone, email, address, credit_limit, opening_balance, current_balance, status) VALUES
(1, 1, 16, 'CUST-001', 'شركة النور التجارية', 'Al-Noor Trading Co.', 'TAX-C001', '+967-1-111111', 'noor@example.com', 'صنعاء', 1000000.00, 0.00, 0.00, 'active'),
(1, 1, 16, 'CUST-002', 'مؤسسة الأمل', 'Al-Amal Foundation', 'TAX-C002', '+967-1-222222', 'amal@example.com', 'عدن', 500000.00, 0.00, 0.00, 'active');

-- ربط العملات (جميع العملات)
INSERT INTO customer_currencies (customer_id, currency_id) VALUES
(1, 1), (1, 2), (1, 3), (1, 4),
(2, 1), (2, 2), (2, 3), (2, 4);
```

#### **3.5 الموردين (Suppliers)**

```sql
INSERT INTO suppliers (unit_id, branch_id, account_id, code, name_ar, name_en, tax_number, phone, email, address, credit_limit, opening_balance, current_balance, status) VALUES
(1, 1, 17, 'SUPP-001', 'شركة الإمداد', 'Supply Company', 'TAX-S001', '+967-1-333333', 'supply@example.com', 'صنعاء', 2000000.00, 0.00, 0.00, 'active'),
(1, 1, 17, 'SUPP-002', 'مؤسسة التوريد', 'Provision Foundation', 'TAX-S002', '+967-1-444444', 'provision@example.com', 'تعز', 1500000.00, 0.00, 0.00, 'active');

-- ربط العملات (جميع العملات)
INSERT INTO supplier_currencies (supplier_id, currency_id) VALUES
(1, 1), (1, 2), (1, 3), (1, 4),
(2, 1), (2, 2), (2, 3), (2, 4);
```

---

## 🧪 سيناريوهات الاختبار

### السيناريو 1: اختبار دليل الحسابات

#### **الهدف:** التحقق من صحة بنية دليل الحسابات الهرمية

#### **الخطوات:**

1. **إنشاء حساب رئيسي جديد**
   - إنشاء حساب "المصروفات الإدارية" تحت "المصروفات"
   - التحقق من عدم ظهور خيارات "العملات المفضلة" و"نوع الحساب التحليلي"

2. **إنشاء حساب فرعي**
   - إنشاء حساب "القرطاسية" تحت "المصروفات الإدارية"
   - تحديد العملات المفضلة (YER فقط)
   - التحقق من إلزامية اختيار العملات

3. **محاولة حذف حساب مرتبط**
   - محاولة حذف حساب "الصناديق" المرتبط بحسابات تحليلية
   - التحقق من منع الحذف

4. **محاولة تغيير نوع حساب مرتبط**
   - محاولة تغيير حساب "الصناديق" من فرعي إلى رئيسي
   - التحقق من منع التغيير

#### **النتائج المتوقعة:**

- ✅ يتم إنشاء الحسابات الرئيسية بدون خيارات العملات
- ✅ يُمنع حفظ الحسابات الفرعية بدون تحديد العملات
- ✅ يُمنع حذف أو تغيير الحسابات المرتبطة بحسابات تحليلية

---

### السيناريو 2: اختبار الحسابات التحليلية

#### **الهدف:** التحقق من صحة ربط الحسابات التحليلية بدليل الحسابات

#### **الخطوات:**

1. **إنشاء صندوق جديد**
   - إنشاء "صندوق الفرع الثاني"
   - ربطه بحساب "الصناديق" في دليل الحسابات
   - تحديد العملات المفضلة (YER فقط)
   - التحقق من وراثة العملات من الحساب الرئيسي

2. **إنشاء بنك جديد**
   - إنشاء "حساب بنك الكريمي"
   - ربطه بحساب "البنوك"
   - اختيار جميع العملات
   - التحقق من إمكانية اختيار جميع أو بعض العملات

3. **إنشاء عميل جديد**
   - إنشاء "شركة الفجر"
   - ربطه بحساب "العملاء"
   - تحديد حد ائتماني
   - التحقق من الرصيد الافتتاحي

#### **النتائج المتوقعة:**

- ✅ يتم ربط الحسابات التحليلية بدليل الحسابات بشكل صحيح
- ✅ يتم وراثة العملات المفضلة من الحساب الرئيسي
- ✅ يمكن اختيار جميع أو بعض العملات المسموحة

---

### السيناريو 3: اختبار القيود اليومية

#### **الهدف:** التحقق من صحة القيود المحاسبية والتوازن

#### **القيد 1: قيد افتتاحي (رأس المال)**

```
التاريخ: 2025-01-01
البيان: قيد افتتاحي - رأس المال

المدين:
- الصندوق الرئيسي (CASH-001): 100,000 YER
- حساب البنك الأهلي (BANK-001): 500,000 YER

الدائن:
- رأس المال: 600,000 YER

الإجمالي: 600,000 YER (متوازن ✅)
```

**SQL:**

```sql
-- رأس القيد
INSERT INTO journal_entries (unit_id, branch_id, entry_number, entry_date, description, total_debit, total_credit, status) VALUES
(1, 1, 'JE-2025-001', '2025-01-01', 'قيد افتتاحي - رأس المال', 600000.00, 600000.00, 'posted');

-- تفاصيل القيد
INSERT INTO journal_entry_lines (entry_id, account_id, analytical_account_type, analytical_account_id, currency_id, debit, credit, exchange_rate, description) VALUES
-- المدين
(1, 13, 'fund', 1, 1, 100000.00, 0.00, 1.00, 'الصندوق الرئيسي'),
(1, 14, 'bank', 1, 1, 500000.00, 0.00, 1.00, 'حساب البنك الأهلي'),
-- الدائن
(1, 7, null, null, 1, 0.00, 600000.00, 1.00, 'رأس المال');
```

#### **القيد 2: سند قبض نقدي (من عميل)**

```
التاريخ: 2025-01-05
البيان: سند قبض من شركة النور التجارية

المدين:
- الصندوق الرئيسي (CASH-001): 50,000 YER

الدائن:
- العملاء - شركة النور (CUST-001): 50,000 YER

الإجمالي: 50,000 YER (متوازن ✅)
```

**SQL:**

```sql
-- سند القبض
INSERT INTO receipt_vouchers (unit_id, branch_id, voucher_number, voucher_date, account_id, analytical_account_type, analytical_account_id, currency_id, amount, exchange_rate, description, status) VALUES
(1, 1, 'RV-2025-001', '2025-01-05', 13, 'fund', 1, 1, 50000.00, 1.00, 'سند قبض من شركة النور التجارية', 'posted');

-- القيد المرتبط
INSERT INTO journal_entries (unit_id, branch_id, entry_number, entry_date, description, total_debit, total_credit, status, voucher_type, voucher_id) VALUES
(1, 1, 'JE-2025-002', '2025-01-05', 'سند قبض من شركة النور التجارية', 50000.00, 50000.00, 'posted', 'receipt', 1);

INSERT INTO journal_entry_lines (entry_id, account_id, analytical_account_type, analytical_account_id, currency_id, debit, credit, exchange_rate, description) VALUES
(2, 13, 'fund', 1, 1, 50000.00, 0.00, 1.00, 'الصندوق الرئيسي'),
(2, 16, 'customer', 1, 1, 0.00, 50000.00, 1.00, 'شركة النور التجارية');
```

#### **القيد 3: سند صرف نقدي (مصروف)**

```
التاريخ: 2025-01-10
البيان: سند صرف - رواتب الموظفين

المدين:
- الرواتب والأجور: 80,000 YER

الدائن:
- الصندوق الرئيسي (CASH-001): 80,000 YER

الإجمالي: 80,000 YER (متوازن ✅)
```

**SQL:**

```sql
-- سند الصرف
INSERT INTO payment_vouchers (unit_id, branch_id, voucher_number, voucher_date, account_id, analytical_account_type, analytical_account_id, currency_id, amount, exchange_rate, description, status) VALUES
(1, 1, 'PV-2025-001', '2025-01-10', 13, 'fund', 1, 1, 80000.00, 1.00, 'سند صرف - رواتب الموظفين', 'posted');

-- القيد المرتبط
INSERT INTO journal_entries (unit_id, branch_id, entry_number, entry_date, description, total_debit, total_credit, status, voucher_type, voucher_id) VALUES
(1, 1, 'JE-2025-003', '2025-01-10', 'سند صرف - رواتب الموظفين', 80000.00, 80000.00, 'posted', 'payment', 1);

INSERT INTO journal_entry_lines (entry_id, account_id, analytical_account_type, analytical_account_id, currency_id, debit, credit, exchange_rate, description) VALUES
(3, 18, null, null, 1, 80000.00, 0.00, 1.00, 'الرواتب والأجور'),
(3, 13, 'fund', 1, 1, 0.00, 80000.00, 1.00, 'الصندوق الرئيسي');
```

#### **القيد 4: قيد بعملة أجنبية (USD)**

```
التاريخ: 2025-01-15
البيان: إيداع في البنك بالدولار

المدين:
- حساب البنك الأهلي (BANK-001): $100 (135,000 YER)

الدائن:
- إيرادات الخدمات: 135,000 YER

الإجمالي: 135,000 YER (متوازن ✅)
سعر الصرف: 1 USD = 1,350 YER
```

**SQL:**

```sql
INSERT INTO journal_entries (unit_id, branch_id, entry_number, entry_date, description, total_debit, total_credit, status) VALUES
(1, 1, 'JE-2025-004', '2025-01-15', 'إيداع في البنك بالدولار', 135000.00, 135000.00, 'posted');

INSERT INTO journal_entry_lines (entry_id, account_id, analytical_account_type, analytical_account_id, currency_id, debit, credit, exchange_rate, description) VALUES
(4, 14, 'bank', 1, 2, 100.00, 0.00, 1350.00, 'حساب البنك الأهلي - USD'),
(4, 21, null, null, 1, 0.00, 135000.00, 1.00, 'إيرادات الخدمات');
```

#### **النتائج المتوقعة:**

- ✅ جميع القيود متوازنة (المدين = الدائن)
- ✅ يتم احتساب أسعار الصرف بشكل صحيح
- ✅ يتم ربط السندات بالقيود تلقائيًا
- ✅ يتم تحديث أرصدة الحسابات التحليلية

---

### السيناريو 4: اختبار التوازن المحاسبي

#### **الهدف:** التحقق من التوازن المحاسبي بعد جميع العمليات

#### **الخطوات:**

1. **حساب إجمالي المدين والدائن**

```sql
SELECT 
    SUM(debit) as total_debit,
    SUM(credit) as total_credit,
    SUM(debit) - SUM(credit) as difference
FROM journal_entry_lines
WHERE entry_id IN (
    SELECT id FROM journal_entries WHERE status = 'posted'
);
```

**النتيجة المتوقعة:**
```
total_debit: 965,000.00
total_credit: 965,000.00
difference: 0.00 ✅
```

2. **التحقق من أرصدة الحسابات التحليلية**

```sql
-- الصندوق الرئيسي
-- الافتتاحي: 100,000
-- + قبض: 50,000
-- - صرف: 80,000
-- = الرصيد الحالي: 70,000

SELECT current_balance FROM funds WHERE id = 1;
-- المتوقع: 70,000.00 ✅

-- حساب البنك الأهلي
-- الافتتاحي: 500,000
-- + إيداع: 135,000 (100 USD)
-- = الرصيد الحالي: 635,000

SELECT current_balance FROM banks WHERE id = 1;
-- المتوقع: 635,000.00 ✅

-- شركة النور التجارية (عميل)
-- الافتتاحي: 0
-- - قبض منه: 50,000
-- = الرصيد الحالي: -50,000 (دائن)

SELECT current_balance FROM customers WHERE id = 1;
-- المتوقع: -50,000.00 ✅
```

#### **النتائج المتوقعة:**

- ✅ إجمالي المدين = إجمالي الدائن
- ✅ أرصدة الحسابات التحليلية محدثة بشكل صحيح
- ✅ لا يوجد فرق في التوازن

---

### السيناريو 5: اختبار قيود العملات

#### **الهدف:** التحقق من تقييد العمليات بالعملات المفضلة

#### **الخطوات:**

1. **محاولة إجراء عملية بعملة غير مسموحة**

```sql
-- محاولة إيداع EUR في الصندوق الرئيسي (يقبل YER فقط)
INSERT INTO journal_entry_lines (entry_id, account_id, analytical_account_type, analytical_account_id, currency_id, debit, credit, exchange_rate) VALUES
(5, 13, 'fund', 1, 4, 100.00, 0.00, 1450.00);
```

**النتيجة المتوقعة:**
```
❌ خطأ: العملة EUR غير مسموحة للحساب "الصندوق الرئيسي"
```

2. **إجراء عملية بعملة مسموحة**

```sql
-- إيداع USD في حساب البنك (يقبل جميع العملات)
INSERT INTO journal_entry_lines (entry_id, account_id, analytical_account_type, analytical_account_id, currency_id, debit, credit, exchange_rate) VALUES
(5, 14, 'bank', 1, 2, 200.00, 0.00, 1350.00);
```

**النتيجة المتوقعة:**
```
✅ تم بنجاح
```

#### **النتائج المتوقعة:**

- ✅ يُمنع إجراء عمليات بعملات غير مسموحة
- ✅ يُسمح بإجراء عمليات بالعملات المفضلة فقط

---

### السيناريو 6: اختبار Multi-Tenancy

#### **الهدف:** التحقق من عزل البيانات بين الوحدات المحاسبية

#### **الخطوات:**

1. **إنشاء وحدة محاسبية ثانية**

```sql
INSERT INTO units (organization_id, branch_id, name_ar, name_en, start_date, fiscal_year_start) VALUES
(1, 1, 'الوحدة المحاسبية الفرعية', 'Sub Accounting Unit', '2025-01-01', '2025-01-01');
```

2. **إنشاء حسابات للوحدة الثانية**

```sql
INSERT INTO chart_of_accounts (unit_id, account_type_id, code, name_ar, name_en, is_parent, level) VALUES
(2, 1, '1', 'الأصول', 'Assets', true, 1);

INSERT INTO funds (unit_id, branch_id, account_id, code, name_ar, name_en, opening_balance, current_balance, status) VALUES
(2, 1, 22, 'CASH-U2-001', 'صندوق الوحدة الثانية', 'Unit 2 Cash Box', 50000.00, 50000.00, 'active');
```

3. **محاولة الوصول إلى بيانات وحدة أخرى**

```sql
-- محاولة إنشاء قيد في الوحدة 1 باستخدام حساب من الوحدة 2
INSERT INTO journal_entries (unit_id, branch_id, entry_number, entry_date, description, total_debit, total_credit, status) VALUES
(1, 1, 'JE-2025-005', '2025-01-20', 'قيد عبر الوحدات', 10000.00, 10000.00, 'posted');

INSERT INTO journal_entry_lines (entry_id, account_id, analytical_account_type, analytical_account_id, currency_id, debit, credit, exchange_rate) VALUES
(5, 22, 'fund', 3, 1, 10000.00, 0.00, 1.00); -- حساب من الوحدة 2
```

**النتيجة المتوقعة:**
```
❌ خطأ: لا يمكن الوصول إلى حسابات وحدة محاسبية أخرى
```

4. **إنشاء معاملة بين الوحدات (Inter-Unit Transaction)**

```sql
-- معاملة من الوحدة 1 إلى الوحدة 2
INSERT INTO inter_unit_transactions (from_unit_id, to_unit_id, transaction_date, amount, currency_id, exchange_rate, description, status) VALUES
(1, 2, '2025-01-20', 100000.00, 1, 1.00, 'تحويل رأس مال للوحدة الثانية', 'approved');
```

**النتيجة المتوقعة:**
```
✅ تم إنشاء المعاملة بين الوحدات بنجاح
```

#### **النتائج المتوقعة:**

- ✅ عزل كامل للبيانات بين الوحدات المحاسبية
- ✅ يُمنع الوصول المباشر إلى حسابات وحدة أخرى
- ✅ يمكن إنشاء معاملات بين الوحدات عبر الآلية المخصصة

---

## ✅ معايير النجاح

### 1. دليل الحسابات

- ✅ يتم إنشاء الحسابات الرئيسية والفرعية بشكل صحيح
- ✅ يُمنع حفظ الحسابات بدون تحديد العملات المفضلة
- ✅ يُمنع حذف أو تغيير الحسابات المرتبطة
- ✅ تظهر خيارات العملات ونوع الحساب التحليلي للحسابات الفرعية فقط

### 2. الحسابات التحليلية

- ✅ يتم ربط الحسابات التحليلية بدليل الحسابات بشكل صحيح
- ✅ يتم وراثة العملات المفضلة من الحساب الرئيسي
- ✅ يمكن اختيار جميع أو بعض العملات المسموحة

### 3. القيود اليومية

- ✅ جميع القيود متوازنة (المدين = الدائن)
- ✅ يتم احتساب أسعار الصرف بشكل صحيح
- ✅ يتم ربط السندات بالقيود تلقائيًا
- ✅ يتم تحديث أرصدة الحسابات التحليلية

### 4. التوازن المحاسبي

- ✅ إجمالي المدين = إجمالي الدائن
- ✅ أرصدة الحسابات التحليلية محدثة بشكل صحيح
- ✅ لا يوجد فرق في التوازن

### 5. قيود العملات

- ✅ يُمنع إجراء عمليات بعملات غير مسموحة
- ✅ يُسمح بإجراء عمليات بالعملات المفضلة فقط

### 6. Multi-Tenancy

- ✅ عزل كامل للبيانات بين الوحدات المحاسبية
- ✅ يُمنع الوصول المباشر إلى حسابات وحدة أخرى
- ✅ يمكن إنشاء معاملات بين الوحدات عبر الآلية المخصصة

---

## 🚀 تنفيذ الاختبار

### الطريقة 1: تنفيذ يدوي عبر SQL

```bash
# 1. الاتصال بقاعدة البيانات
mysql -u alabasi -palabasi123 alabasi_unified

# 2. تنفيذ البيانات التجريبية
source /path/to/test_data.sql

# 3. تنفيذ سيناريوهات الاختبار
source /path/to/test_scenarios.sql

# 4. التحقق من النتائج
source /path/to/verify_results.sql
```

### الطريقة 2: تنفيذ آلي عبر سكريبت

```bash
# إنشاء سكريبت الاختبار
cd /home/ubuntu/alabasi-unified-system
node scripts/run-accounting-tests.js
```

### الطريقة 3: تنفيذ عبر الواجهة

1. تسجيل الدخول إلى النظام
2. الانتقال إلى "دليل الحسابات"
3. تنفيذ السيناريوهات يدويًا
4. التحقق من النتائج

---

## 📈 التقارير المطلوبة

بعد تنفيذ الاختبار، يجب إنشاء التقارير التالية:

### 1. تقرير ميزان المراجعة (Trial Balance)

```sql
SELECT 
    coa.code,
    coa.name_ar,
    SUM(jel.debit) as total_debit,
    SUM(jel.credit) as total_credit,
    SUM(jel.debit) - SUM(jel.credit) as balance
FROM journal_entry_lines jel
JOIN chart_of_accounts coa ON jel.account_id = coa.id
JOIN journal_entries je ON jel.entry_id = je.id
WHERE je.status = 'posted' AND je.unit_id = 1
GROUP BY coa.id
ORDER BY coa.code;
```

### 2. تقرير كشف الحساب (Account Statement)

```sql
SELECT 
    je.entry_date,
    je.entry_number,
    je.description,
    jel.debit,
    jel.credit,
    @balance := @balance + jel.debit - jel.credit as balance
FROM journal_entry_lines jel
JOIN journal_entries je ON jel.entry_id = je.id
CROSS JOIN (SELECT @balance := 0) vars
WHERE jel.account_id = 13 -- الصندوق الرئيسي
  AND je.status = 'posted'
ORDER BY je.entry_date, je.id;
```

### 3. تقرير الأرصدة التحليلية

```sql
-- الصناديق
SELECT code, name_ar, opening_balance, current_balance FROM funds WHERE unit_id = 1;

-- البنوك
SELECT code, name_ar, opening_balance, current_balance FROM banks WHERE unit_id = 1;

-- العملاء
SELECT code, name_ar, opening_balance, current_balance FROM customers WHERE unit_id = 1;

-- الموردين
SELECT code, name_ar, opening_balance, current_balance FROM suppliers WHERE unit_id = 1;
```

---

## 🎓 الخلاصة

هذا السيناريو يوفر اختبارًا شاملاً لوحدة المحاسبة في نظام العباسي الموحد، مع التركيز على:

- ✅ **دليل الحسابات** الهرمي المرن
- ✅ **الحسابات التحليلية** المتكاملة
- ✅ **القيود اليومية** المتوازنة
- ✅ **العملات المتعددة** مع أسعار الصرف
- ✅ **Multi-Tenancy** مع عزل البيانات
- ✅ **سلامة البيانات** وتكاملها

تنفيذ هذا السيناريو بنجاح يضمن أن وحدة المحاسبة جاهزة للإنتاج والاستخدام الفعلي.

---

**تم إعداد السيناريو بنجاح! 🎉**
