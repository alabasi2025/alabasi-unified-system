# 🪟 دليل التثبيت على Windows 10

## 🎯 نظرة عامة

هذا الدليل يشرح كيفية تثبيت **نظام العباسي الموحد** على **Windows 10** بطريقة بسيطة وسهلة.

---

## ✅ طريقة التثبيت السريعة (موصى بها)

### **الخطوة 1: تحميل النظام**

1. اذهب إلى: https://github.com/alabasi2025/alabasi-unified-system
2. انقر على الزر الأخضر **Code**
3. اختر **Download ZIP**
4. فك ضغط الملف في أي مكان تريده

### **الخطوة 2: تشغيل برنامج التثبيت**

1. افتح مجلد `installer`
2. انقر بزر الماوس الأيمن على `Install-Windows10.bat`
3. اختر **تشغيل كمسؤول** (Run as Administrator)
4. اتبع التعليمات على الشاشة

### **الخطوة 3: الانتظار**

البرنامج سيقوم تلقائيًا بـ:
- ✅ فحص المتطلبات المسبقة
- ✅ تثبيت Node.js (إذا لم يكن مثبتًا)
- ✅ تثبيت MySQL (إذا لم يكن مثبتًا)
- ✅ تثبيت Git (إذا لم يكن مثبتًا)
- ✅ تحميل النظام من GitHub
- ✅ إعداد قاعدة البيانات
- ✅ تثبيت المكتبات
- ✅ إنشاء اختصار على سطح المكتب

### **الخطوة 4: تشغيل النظام**

بعد اكتمال التثبيت:
1. انقر نقرًا مزدوجًا على اختصار **نظام العباسي الموحد** على سطح المكتب
2. سيفتح المتصفح تلقائيًا على: http://localhost:3000

**بيانات الدخول:**
- البريد الإلكتروني: `admin@alabasi.com`
- كلمة المرور: `admin123`

---

## 🔧 طريقة التثبيت اليدوية (للمتقدمين)

إذا كنت تفضل التثبيت اليدوي، اتبع الخطوات التالية:

### **1. تثبيت المتطلبات يدويًا**

#### **Node.js:**
1. اذهب إلى: https://nodejs.org
2. حمّل الإصدار LTS (الموصى به)
3. شغّل الملف المحمّل واتبع التعليمات

#### **MySQL:**
1. اذهب إلى: https://dev.mysql.com/downloads/mysql/
2. حمّل MySQL Community Server
3. شغّل الملف المحمّل واتبع التعليمات
4. احفظ كلمة مرور المستخدم `root`

#### **Git:**
1. اذهب إلى: https://git-scm.com/downloads
2. حمّل Git for Windows
3. شغّل الملف المحمّل واتبع التعليمات

### **2. تحميل النظام**

افتح **Command Prompt** أو **PowerShell** واكتب:

```cmd
cd C:\
git clone https://github.com/alabasi2025/alabasi-unified-system.git
cd alabasi-unified-system
```

### **3. تثبيت pnpm**

```cmd
npm install -g pnpm
```

### **4. تثبيت المكتبات**

```cmd
pnpm install
```

### **5. إعداد قاعدة البيانات**

افتح **MySQL Command Line Client** واكتب:

```sql
CREATE DATABASE alabasi_unified CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'alabasi'@'localhost' IDENTIFIED BY 'alabasi123';
GRANT ALL PRIVILEGES ON alabasi_unified.* TO 'alabasi'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

ثم ارجع إلى Command Prompt واكتب:

```cmd
mysql -u alabasi -palabasi123 alabasi_unified < create_tables.sql
mysql -u alabasi -palabasi123 alabasi_unified < comprehensive_test_data.sql
```

### **6. إنشاء ملف .env**

أنشئ ملف اسمه `.env` في مجلد النظام واكتب فيه:

```env
DATABASE_URL=mysql://alabasi:alabasi123@localhost:3306/alabasi_unified
PORT=3000
NODE_ENV=development
JWT_SECRET=your-secret-key-change-this
GITHUB_REPO=alabasi2025/alabasi-unified-system
GITHUB_BRANCH=main
```

### **7. تشغيل النظام**

```cmd
pnpm dev
```

افتح المتصفح واذهب إلى: http://localhost:3000

---

## 🛠️ استكشاف الأخطاء

### **المشكلة: "لا يمكن تشغيل البرنامج كمسؤول"**

**الحل:**
1. انقر بزر الماوس الأيمن على `Install-Windows10.bat`
2. اختر **خصائص** (Properties)
3. اذهب إلى تبويب **التوافق** (Compatibility)
4. ضع علامة على **تشغيل هذا البرنامج كمسؤول**
5. انقر **موافق**

### **المشكلة: "MySQL لا يعمل"**

**الحل:**
1. افتح **Services** (خدمات Windows)
2. ابحث عن **MySQL**
3. انقر بزر الماوس الأيمن واختر **Start** (بدء)

### **المشكلة: "المنفذ 3000 مستخدم"**

**الحل:**
1. افتح ملف `.env`
2. غيّر `PORT=3000` إلى `PORT=3001`
3. احفظ الملف وشغّل النظام مرة أخرى

### **المشكلة: "خطأ في تثبيت المكتبات"**

**الحل:**
```cmd
cd C:\alabasi-unified-system
rmdir /s /q node_modules
pnpm install
```

---

## 🔄 التحديث

لتحديث النظام إلى أحدث إصدار:

### **طريقة 1: باستخدام برنامج التحديث**

1. افتح مجلد `C:\AlaabasiSystem\installer`
2. انقر نقرًا مزدوجًا على `Update.bat`

### **طريقة 2: يدويًا**

```cmd
cd C:\AlaabasiSystem
git pull origin main
pnpm install
```

---

## 🗑️ إلغاء التثبيت

لإزالة النظام بالكامل:

1. احذف مجلد `C:\AlaabasiSystem`
2. احذف اختصار سطح المكتب
3. (اختياري) أزل MySQL و Node.js من **لوحة التحكم > البرامج**

---

## 📞 الدعم

إذا واجهت أي مشكلة:
- **المستودع:** https://github.com/alabasi2025/alabasi-unified-system
- **المشاكل:** https://github.com/alabasi2025/alabasi-unified-system/issues

---

## ✨ مميزات النظام

- ✅ **تثبيت تلقائي** لجميع المتطلبات
- ✅ **واجهة عربية** كاملة
- ✅ **تحديث تلقائي** من GitHub
- ✅ **بيانات تجريبية** جاهزة
- ✅ **نظام محاسبي متكامل**
- ✅ **إدارة المخزون**
- ✅ **المبيعات والمشتريات**
- ✅ **إدارة الطاقة**
- ✅ **ذكاء اصطناعي**

---

**تم إنشاء هذا الدليل بواسطة:** Manus AI  
**آخر تحديث:** 13 نوفمبر 2025
