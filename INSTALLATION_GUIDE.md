# 📦 دليل التثبيت والتشغيل - نظام العباسي الموحد

## 🎯 نظرة عامة

هذا الدليل يشرح كيفية تثبيت وتشغيل **نظام العباسي الموحد** على جهازك المحلي (Windows/Linux/Mac).

---

## ✅ المتطلبات المسبقة

قبل البدء، تأكد من تثبيت البرامج التالية:

| البرنامج | الإصدار المطلوب | رابط التحميل |
|----------|------------------|---------------|
| **Node.js** | 18.x أو أحدث | https://nodejs.org |
| **MySQL** | 8.0 أو أحدث | https://dev.mysql.com/downloads/mysql/ |
| **Git** | أي إصدار | https://git-scm.com/downloads |
| **pnpm** | أي إصدار | `npm install -g pnpm` |

---

## 🚀 طريقة التثبيت السريعة (للمستخدمين)

### **الخطوة 1: تحميل النظام**

```bash
git clone https://github.com/alabasi2025/alabasi-unified-system.git
cd alabasi-unified-system
```

### **الخطوة 2: تثبيت المكتبات**

```bash
pnpm install
```

### **الخطوة 3: إعداد قاعدة البيانات**

```bash
# إنشاء قاعدة البيانات
mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS alabasi_unified CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# إنشاء مستخدم خاص
mysql -u root -p -e "CREATE USER IF NOT EXISTS 'alabasi'@'localhost' IDENTIFIED BY 'alabasi123';"
mysql -u root -p -e "GRANT ALL PRIVILEGES ON alabasi_unified.* TO 'alabasi'@'localhost';"
mysql -u root -p -e "FLUSH PRIVILEGES;"

# تطبيق الجداول
mysql -u alabasi -palabasi123 alabasi_unified < create_tables.sql

# تحميل البيانات التجريبية
mysql -u alabasi -palabasi123 alabasi_unified < comprehensive_test_data.sql
```

### **الخطوة 4: إعداد ملف البيئة**

```bash
# إنشاء ملف .env
cat > .env << 'EOF'
# Database
DATABASE_URL=mysql://alabasi:alabasi123@localhost:3306/alabasi_unified

# Server
PORT=3000
NODE_ENV=development

# JWT
JWT_SECRET=your-secret-key-change-this-in-production

# GitHub (للتحديثات التلقائية)
GITHUB_REPO=alabasi2025/alabasi-unified-system
GITHUB_BRANCH=main
EOF
```

### **الخطوة 5: تشغيل النظام**

```bash
# تشغيل البيئة التطويرية
pnpm dev
```

**النظام الآن يعمل على:**
- Frontend: http://localhost:5173
- Backend: http://localhost:3000

---

## 🔧 التثبيت المتقدم (باستخدام نظام التثبيت الذكي)

### **Windows:**

```powershell
# تشغيل برنامج التثبيت
cd installer
.\Install.bat
```

سيقوم البرنامج بـ:
1. ✅ فحص المتطلبات المسبقة
2. ✅ تثبيت ما هو مفقود تلقائيًا
3. ✅ إعداد قاعدة البيانات
4. ✅ تحميل البيانات التجريبية
5. ✅ تشغيل النظام

### **Linux/Mac:**

```bash
# تشغيل سكريبت التثبيت
cd installer
chmod +x Install-AccountingSystem.ps1
pwsh Install-AccountingSystem.ps1
```

---

## 📊 البيانات التجريبية

النظام يأتي مع بيانات تجريبية جاهزة للاختبار:

| البيانات | الكمية |
|----------|---------|
| العملات | 4 |
| دليل الحسابات | 22 حساب |
| الحسابات التحليلية | 10 حسابات |
| المخازن | 3 |
| الأصناف | 6 |
| فواتير المبيعات | 2 |
| فواتير المشتريات | 1 |
| محطات الطاقة | 3 |
| الموظفين | 4 |

**بيانات الدخول التجريبية:**
- البريد الإلكتروني: `admin@alabasi.com`
- كلمة المرور: `admin123`

---

## 🔄 التحديث التلقائي

النظام يدعم التحديث التلقائي من GitHub:

```bash
# تحديث يدوي
cd installer
.\Update.bat

# أو استخدام Git مباشرة
git pull origin main
pnpm install
```

**التحديث التلقائي:**
- يتم فحص التحديثات كل 24 ساعة
- إشعار تلقائي عند توفر تحديث جديد
- تحديث بنقرة واحدة من واجهة النظام

---

## 🛠️ استكشاف الأخطاء

### **المشكلة: خطأ في الاتصال بقاعدة البيانات**

```bash
# تحقق من تشغيل MySQL
sudo systemctl status mysql  # Linux
net start MySQL  # Windows

# تحقق من بيانات الاتصال في .env
cat .env | grep DATABASE_URL
```

### **المشكلة: المنفذ 3000 مستخدم**

```bash
# تغيير المنفذ في .env
echo "PORT=3001" >> .env
```

### **المشكلة: خطأ في تثبيت المكتبات**

```bash
# حذف node_modules وإعادة التثبيت
rm -rf node_modules
pnpm install
```

---

## 📦 بناء النظام للإنتاج

```bash
# بناء Frontend
cd client
pnpm build

# بناء Backend
cd ../server
pnpm build

# تشغيل الإنتاج
NODE_ENV=production pnpm start
```

---

## 🌐 نشر النظام

### **Docker:**

```bash
# بناء الصورة
docker build -t alabasi-unified-system .

# تشغيل الحاوية
docker run -p 3000:3000 -p 5173:5173 alabasi-unified-system
```

### **PM2 (للخوادم):**

```bash
# تثبيت PM2
npm install -g pm2

# تشغيل النظام
pm2 start server/index.js --name alabasi-system
pm2 save
pm2 startup
```

---

## 📞 الدعم والمساعدة

- **المستودع:** https://github.com/alabasi2025/alabasi-unified-system
- **التوثيق:** https://github.com/alabasi2025/alabasi-unified-system/wiki
- **المشاكل:** https://github.com/alabasi2025/alabasi-unified-system/issues

---

## 📝 ملاحظات مهمة

1. **الأمان:** غيّر `JWT_SECRET` في ملف `.env` قبل النشر للإنتاج
2. **النسخ الاحتياطي:** احتفظ بنسخة احتياطية من قاعدة البيانات بشكل دوري
3. **التحديثات:** تحقق من التحديثات بانتظام للحصول على أحدث المميزات والإصلاحات

---

**تم إنشاء هذا الدليل بواسطة:** Manus AI  
**آخر تحديث:** 13 نوفمبر 2025
